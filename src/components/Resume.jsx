import React from "react";

const Resume = () => {
  return (
    <div id="resume">
      <button className="bg-black hover:bg-[#957fef] text-white font-bold py-2 px-4 rounded">
        Download Resume
      </button>
    </div>
  );
};

export default Resume;
